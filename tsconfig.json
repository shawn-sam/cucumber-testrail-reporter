{
    "compilerOptions": 
        {
        "target": "ES6",
        "module": "CommonJS", // or "ES6" if you're using ES6 modules
        "declaration": true,
        "outDir": "./dist",
        // Other compiler options as needed
        },
    "include": [
    "src/**/*.ts" // Adjust the path to match your plugin's TypeScript source files
    ],
    "exclude": [
    "node_modules"
    ]
}